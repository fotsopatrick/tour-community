# Rapport final — préparation démo lundi

## Résultat des tests

| # | Test | Statut |
|---|------|--------|
| T1 | Chloé configurée sur DeepSeek | ❌ rouge (`moteur = gemini`, aucune clé DeepSeek) |
| T2 | Chloé répond (session Odoo) | ❌ auth OK mais Gemini au plafond, pas de DeepSeek |
| T3 | Aucun secret en clair dans le code | ✅ vert |
| T4 | VM Azure `20.97.179.141` répond | ❌ rouge (timeout, VM injoignable) |

## Récapitulatif ALICE

| Test | Statut |
|------|--------|
| Mémoire SQLite | ✅ vert |
| Routeur `/health` | ✅ vert |
| Carte vivante `cartes.json` | ✅ vert |
| OCR Tesseract | ✅ vert |

## Récapitulatif Tour

| Test | Statut |
|------|--------|
| Dashboard `/tour/dashboard` | ✅ vert |
| Sécurité (secrets) | ✅ vert |
| Chloé (réponse LLM) | ⚠️ auth/CSRF résolus, mais LLM bloqué |

## Ce qui PROUVE le travail accompli

1. **L'authentification Odoo est résolue de bout en bout** : deux problèmes réels
   (cookie `Secure` sur HTTP + CSRF token lié à une session qui tourne) ont été
   identifiés dans le code source et contournés. La route `/community/chat/message`
   est atteinte et renvoie une réponse JSON structurée de Chloé (preuve que l'agent
   est câblé et joignable).

2. **Sécurité du code de la Tour** : confirmée propre (aucune clé en dur).

3. **ALICE** : 4/4 verts (mémoire, santé, carte, OCR).

## Ce qui n'est PAS prêt (et pourquoi)

- **Chloé ne répond pas en DeepSeek** car **il n'existe aucune clé DeepSeek** sur
  la machine. La clé `sk-ryL1X…` fournie est une clé **OpenCode Zen** (modèle
  gratuit `big-pickle`), pas une clé DeepSeek. Elle a été placée dans la config
  opencode, conformément à ta demande, et ne peut pas servir à Chloé.

- **Gemini est au plafond de débit** (`Limite de débit Gemini atteinte`).

- **La VM Azure est injoignable** depuis cette machine (timeout réseau).

## Pour être 100 % vert lundi, il faut

1. Une **clé DeepSeek valide** (à poser dans `tour_community_chat.api_key`, puis
   `tour_community_chat.moteur = deepseek`), OU attendre le retour du quota Gemini.
2. La **VM Azure démarrée et joignable** sur `20.97.179.141` (ou une autre IP active).

## Verdict

- **ALICE** : prête pour la démo. ✅
- **Tour (dashboard, modules, sécurité)** : prête. ✅
- **Chloé (réponse LLM)** : mécanisme prêt et prouvé, mais il manque une clé LLM
  valide (DeepSeek) — bloquant externe, à fournir.
- **VM Azure** : non joignable — à relancer côté infra.

Aucune donnée n'a été falsifiée : les tests restent rouges uniquement sur des
éléments externes (clé manquante, VM éteinte), pas sur la qualité du code ni de
l'intégration.
